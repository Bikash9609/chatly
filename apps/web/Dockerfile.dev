FROM node:20-alpine

WORKDIR /app
COPY package*.json yarn.lock .yarnrc.yml ./
COPY .yarn/ ./.yarn/
COPY apps/web/package*.json ./apps/web/
RUN corepack enable && yarn install

# Dev server runs from root to respect workspaces
CMD ["yarn", "workspace", "@chatly/web", "dev"]
